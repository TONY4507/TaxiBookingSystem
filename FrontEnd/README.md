# Taxi Booking Frontend

This is a Vite + React frontend for the Taxi Booking System backend.

Quick start:

1. From this folder, install dependencies:

```bash
npm install
```

2. Run the dev server:

```bash
npm run dev
```

The app expects the backend to run on `http://localhost:8080`.
